
{{- if .Values.ingress_scheduler.enabled -}}
{{- $release_name := .Release.Name -}}
{{- $deployment_scheduler := .Values.deployment_scheduler.name -}}
{{- $svcPort := .Values.service_scheduler.port -}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-{{ .Values.ingress_scheduler.name }}
  {{- with .Values.ingress_scheduler.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress_scheduler.tls }}
  tls:
    {{- range .Values.ingress_scheduler.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}

  rules:
    {{- range .Values.ingress_scheduler.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name:  {{  $release_name }}-{{  $deployment_scheduler }}
                port:
                  number: {{  $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}